#ifndef CONFIG_H
#define CONFIG_H

// host address
static const char* host = "127.0.0.1";

// host port
static uint16_t port = 3004;

// maximum concurrent connections to server
static const int max_connections = 4;

#endif // CONFIG_H